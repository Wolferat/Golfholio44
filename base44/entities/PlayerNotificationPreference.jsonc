{
  "name": "PlayerNotificationPreference",
  "type": "object",
  "properties": {
    "push_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Explicit opt-in for future device push notifications. Default false \u2014 never prompted on launch, signup, or first search."
    }
  },
  "required": [],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}